'use client';

import { Box, Typography } from '@mui/material';
import React from 'react';

export default function MapsPage() {
    return (
        <Box
            sx={{
                background: 'white',
            }}
        >
            <Typography variant="h6">맵</Typography>
        </Box>
    );
}
