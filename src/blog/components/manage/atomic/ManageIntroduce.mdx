import { Alert, Button, Grid, Box, Typography } from '@mui/material';
import Link from 'next/link';
import { URL_MAP } from '@/common/URL';
import { createTheme } from '@mui/material/styles';

# 소개

<Alert severity="info">관리자 페이지입니다.</Alert>

<Grid
    container
    spacing={2}
    sx={{
        mt: 4,
    }}
>
    <Grid item xs={12}>
        <Typography variant="h5">메뉴</Typography>
    </Grid>
    <Grid item xs={12} sm={12} md={6} lg={4}>
        <Button
            variant="contained"
            color="primary"
            fullWidth
            href={URL_MAP.ADMIN_CATEGORY}
            LinkComponent={Link}
        >
            카테고리 관리
        </Button>
    </Grid>
    <Grid item xs={12} sm={12} md={6} lg={4}>
        <Button
            variant="contained"
            color="primary"
            fullWidth
            href={URL_MAP.ADMIN_BLOG_INFO}
            LinkComponent={Link}
        >
            기본 정보 설정
        </Button>
    </Grid>
    <Grid item xs={12} sm={12} md={6} lg={4}>
        <Button
            variant="contained"
            color="secondary"
            theme={createTheme({
                palette: {
                    primary: {
                        main: '#f44336',
                    },
                    secondary: {
                        main: '#890b44',
                    },
                },
            })}
            fullWidth
            href={URL_MAP.MAIN}
            LinkComponent={Link}
        >
            메인 화면
        </Button>
    </Grid>
</Grid>
